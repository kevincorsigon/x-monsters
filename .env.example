# Variáveis de ambiente para docker-compose.yml
# Copie este arquivo para .env e altere conforme necessário.
# Valores padrão já funcionam para uso local.

# Host/Port
# HOST_PORT=8000

# Diretório de matches (já mapeado internamente para o volume xmonsters_matches)
# Nenhuma variável extra é necessária para persistência — o volume é gerenciado
# automaticamente pelo Docker Compose.
