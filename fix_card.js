const fs = require('fs'); let content = fs.readFileSync('src/js/card-rules.js', 'utf8'); content = content.replace('const rule = card ? getRule(card.definitionId) : null;', 'const rule = card ? getRule(card.definitionId) : null;\\n                if (card && card.modifiers) {\\n                    card.modifiers = card.modifiers.filter(m => !m.id.includes(ree_resummon));\\n                }'); fs.writeFileSync('src/js/card-rules.js', content);
